DB_USERNAME=your_username
DB_PASSWORD=your_password
SPRING_DATASOURCE_URL=jdbc:postgresql://your_db_host:your_port/RSSwithAI

# JWT secret key for signing and verifying tokens, make sure to use a strong and secure key in production
JWT_SECRET=your_jwt_secret_key
